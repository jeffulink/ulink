<%= form_for(product) do |f| %>

    <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

<div class="form-group"><%= f.file_field :logo %></div>
<div class="form-group"><%= f.file_field :logo %></div>


  <%= f.label :category %>
  <%= f.collection_select(:category_id, Category.all, :id, :category) %>
  
  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>