<h1>產品資料表</h1>



<table class="table">
    <thead>
        <tr>
            <th>Name</th>
            <th>Category</th>
            <th>備註</th>
        </tr>
    </thead>
    <!-- 是一般 html 註解法 -->
    <tbody>
      <% @products.each do |product| %>
        <tr>
            <td><%= product.name %></td>
            <td><%= product.category %></td>
            <td><%= product.note %></td>
<%
=begin %><td><%= link_to 'Show', user, class: 'btn btn-info' %></td>
            <td><%= link_to 'Edit', edit_product_path(user), class:'btn btn-primary' %></td>
            <td><%= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' }, class:'btn btn-danger' %></td>
<%
=end %>
        <tr>
      <% end %>
    </tbody>
</table>

<br>

  <%= paginate @products %>

<br>
